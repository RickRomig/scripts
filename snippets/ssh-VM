# SSH on NAT VM 
# Run from VM to allow transfers from host machine.
# Generate ssh key
ssh-keygen
# Copy the key to the host machine
ssh-copy-id rick@192.168.122.1
# Initial copy of host's ~/bin directory
rsync -avh --delete rick@192.168.122.1:bin $HOME/bin/
