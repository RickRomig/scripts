while true; do
	read -rp "Do you wish to continue? [Yes|No]" yesno
	shopt -s nocasematch
	case "$yesno" in
		yes ) echo "Continue..."; shopt -u nocasematch; break ;;
		no ) echo "Script canceled." shopt -u nocasematch; break ;;
		* ) echo "Invalide choice. Must enter Yes or No" ;;
	esac
done
