#!/usr/bin/env bash
##########################################################################
# Script Name  : install-nerdfonts
# Description  : installs nerdfonts
# Dependencies : wget unzip
# Arguments    : none
# Author       : Copyright © 2023 Richard B. Romig, Mosfanet
# Email        : rick.romig@gmail.com | rick.romig@mymetronet.net
# Created      : 28 Sep 2023
# Last updated : 16 May 2024
# Comments     :
# TODO (Rick)  :
# License      : GNU General Public License, version 2.0
##########################################################################

## Shellcheck Directives ##
# shellcheck source=/home/rick/bin/functionlib

## Source function library ##

if [[ -x "$HOME/bin/functionlib" ]]; then
  source "$HOME/bin/functionlib"
else
  printf "\e[91mERROR:\e[0m functionlib not found!\n" >&2
  exit 1
fi

## Global Variables ##

_script=$(basename "$0"); readonly _script
readonly _version="1.3.24137"
readonly font_dir="/usr/share/fonts"

## Functions ##

check_dependencies() {
  local packages=( wget unzip )
  check_packages "${packages[@]}"
}

# shellcheck disable=SC2317 # Don't warn about unreachable commands in this function
# ShellCheck may incorrectly believe that code is unreachable if it's invoked by variable name or in a trap.
cleanup() {
    [[ -d "$tmp_dir" ]] && rm -rf "$tmp_dir"
}

install_nerd_fonts() {
  local font
  local fonts=( "CascadiaCode" "FiraCode" "Go-Mono" "Hack" "Inconsolata" "Iosevka" "JetBrainsMono" "Mononoki" "RobotoMono" "SourceCodePro" )
  local font_repo="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.2.1"
  for font in "${fonts[@]}"; do
	  wget -P "$tmp_dir" "$font_repo/$font.zip"
	  sudo unzip "$tmp_dir/$font.zip" -d "$font_dir/$font/"
  done
  fc-cache
  printf "Nerd fonts installed.\n"
}

## Execution ##

sudo_login 2
check_dependencies

[[ -d "$font_dir" ]] || sudo mkdir -p "$font_dir"
tmp_dir=$(mktemp -d) || { printf "%s Failed to create temporary directory.\n" "$red_error" >&2; exit 1; }

trap cleanup EXIT
install_nerd_fonts
leave "$_script v$_version"
