# user_exists
### Purpose
Checks if the user passed to the function exists in `/etc/passwd`.
### Arguments
$1 -> Username to check in /etc/passwd
### Returns
TRUE (0) if the user exists, ohterwise FALSE (1)
### Usage
```bash
user_exists Bob
```
### Notes
