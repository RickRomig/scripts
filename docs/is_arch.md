# is_arch
### Purpose
Check if the distribution is based on Arch Linux.
### Arguments
None
### Returns
TRUE (0) if an Arch-based distribution, FALSE (1) if not.
### Usage
```bash
is_arch && pacman -S
```
### Notes
