#!/usr/bin/env bash
#####################################################################
# Script Name  : install-homebank
# Description  : Installs Homebank from PPA or distribution reposistory
# Dependencies : none
# Arguments    : none
# Author       : Copyright (C) 2018, Richard Romig, 29 November 2018
# Email        : rick.romig@gmail.com | rick.romig@mymetronet.net
# Comments     : Available as a flatpak
#              : https://flathub.org/apps/details/fr.free.Homebank
#              : Linux Mint Sofware Manager
# TODO (rick)  :
# License      : GNU General Public License, version 2.0
###############################################################################

## Shellcheck Directives ##
# shellcheck source=/home/rick/bin/functionlib
# shellcheck disable=SC1091,SC2034

## Load function library ##

if [[ -x "$HOME/bin/functionlib" ]]; then
  source "$HOME/bin/functionlib"
else
  printf "\e[91mERROR:\e[0m functionlib not found!\n" >&2
  exit 1
fi

## Variables ##

_script=$(basename "$0"); readonly _script
readonly _version="0.3.5"
readonly _updated="29 Mar 2023"

## Functions ##

homebank_ver() {
  homebank --version | cut -d' ' -f2
}

add_ppa() {
  echo "Adding repository and installing HomeBank..."
  sudo add-apt-repository ppa:mdoyen/homebank
}

remove_ppa() {
  sudo add-apt-repository --remove ppa:mdoyen/homebank
}

install_homebank() {
  user_in_sudo
  support_ppa && add_ppa
  sudo apt-get install homebank -yyq
  echo "HomeBank $(homebank_ver) installed."
}

remove_homebank() {
  echo "Removing HomeBank $(homebank_ver)..."
  user_in_sudo
  support_ppa && remove_ppa
  sudo apt-get remove homebank -yy > /dev/null 2>&1
  echo "HomeBank has been removed."
}

purge_homebank() {
  echo "Removing HomeBank $(homebank_ver) and purging configuration files."
  user_in_sudo
  support_ppa && remove_ppa
  sudo apt-get purge homebank -yy > /dev/null 2>&1
  echo "HomeBank and its configuration files have been removed."
}

help() {
  local errcode="${1:-2}"
  cat << END_HELP
${green}Usage:${normal} $_script [-h] [-i] [-r]
${orange}OPTIONS:${normal}
  -h  Display help
  -i  Install HomeBank from PPA or repository
  -p  Remove HomeBank and purge configuration files (PPA or repository)
  -r  Remove HomeBank (PPA or repository)
      HomeBank is also available as a Flatpak (Flathub or Mint Softwware Manager)
END_HELP
  echo "$_script v$_version ($_updated)"
  exit "$errcode"
}

## Execution ##

clear
echo "Installs HomeBank Personal Finance Software."

noOpt=1
optstr=":hipr"
while getopts "$optstr" opt; do
  case "$opt" in
    h )
      help 0 ;;
    i )
      exists homebank && leave "HomeBank $(homebank_ver) is already installed."
      install_homebank ;;
    p )
      exists hombank || leave "HomeBank is not installed."
      purge_homebank ;;
    r )
      exists hombank || leave "HomeBank is not installed."
      remove_homebank ;;
    ? )
      echo "${lightred}ERROR:${normal} Invalid option -$OPTARG" >&2
      help 2 ;;
  esac
	noOpt=0
done
if [ $noOpt = 1 ];then echo "${lightred}ERROR:${normal} No argument passed." >&2;help 1;fi
echo "$_script v$_version ($_updated)"
exit
